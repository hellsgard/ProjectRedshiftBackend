// const router = require('express').Router();
// const { QueryTypes } = require('sequelize');
// const sequelize = require('../utils/database.js');


// router.get('/readAll', async (req, res) => {
//     const vehicleObservations = await sequelize.query("SELECT * FROM bankcard LIMIT 10", { type: sequelize.QueryTypes.SELECT } ); // this will need changing back to citizen
//     console.log(vehicleObservations);
//     res.status(200).send(vehicleObservations);
//     });








// module.exports = router;